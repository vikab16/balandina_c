Лабораторная работа 2: 
Структуры в языке C

Описание проекта: 

 Цель работы: Изучить объявление и использование структур, вложенные структуры, указатели на структуры, массивы структур, передачу структур в функции, перечисления (enum) и объединения (union).

Основные задачи:

Базовые структуры: Объявление, инициализация и работа с простыми структурами.
Массивы структур:  Создание и обработка коллекций структур.
Вложенные структуры: Использование одной структуры внутри другой.
Передача структур в функции: Различные способы передачи (по значению, по указателю) и их применение.
Перечисления (enum)
Объединения (union): Эффективное использование памяти для хранения разных типов данных в одном месте с механизмом отслеживания типа.
Комплексное управление данными: Создание системы управления записями с использованием массивов структур, функций, указателей и "enum".

Используемые технологии и библиотеки

Язык программирования: C
Компилятор: стандартный компилятор C 
Стандартные библиотеки C:
    •   stdio.h для операций ввода/вывода.
    •   string.h для работы со строками.

Инструкция по сборке и запуску:

1.Сохранить все файлы в одной директории.
2.Перейти в директорию проекта.
3.Скомпилировать исходные файлы с помощью компилятора C.
gcc file1.c -o test1
4.Запустить скомпилированную программу: ./test1

Ответы на контрольные вопросы:

Как объявить структуру в языке C: 
1. Для объявления структуры используется ключевое слово struct.
   struct Student {
    char name[50];
    int age;
    float grade;
  };
2. Что такое вложенные структуры и зачем они нужны 
Вложенные структуры — это структуры, которые содержат другие структуры в качестве своих полей. Они нужны для логической группировки связанных данных.

struct Date {
    int day;
    int month;
    int year;
};

Основная структура студента с вложенной структурой

struct Student {
    char name[50];
    struct Date birth_date;  
    float average_grade;
};

3. Как передать структуру в функцию 

Структуру можно передать в функцию двумя основными способами:
  1. По значению (by value): Передается копия всей структуры. 
void printStudent(struct Student s) {
            printf("Name: %s, Age: %d\n", s.name, s.age);
        }
  2. По указателю (by pointer): Передается адрес структуры.

void updateAge(struct Student *s, int newAge) {
            s->age = newAge; 
        }

4. Можно ли создавать массивы структур 
Да, можно.
struct Student students[5]; 

5. Как обратиться к элементу массива структур 

Используя индексацию массива и оператор точки . 

printf("Имя первого студента: %s\n", students[0].name);
printf("Балл второго студента: %.2f\n", students[1].average_grade);

6. Можно ли использовать enum внутри структуры 
Да, можно.

enum Course {first, seconf };

struct Student {
    char name[50];
    int age;
    enum Course course;  
    float average_grade;
};

7. Можно ли сравнивать структуры оператором == 

Нет, напрямую сравнивать структуры оператором == нельзя. Компилятор выдаст ошибку.

Оператор == не определён для составных типов данных. Компилятор не знает, как сравнивать все поля структуры одновременно.



