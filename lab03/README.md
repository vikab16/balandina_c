    Лабораторная работа №3
    Динамические массивы в языке C

    Цель работы:

Изучить работу с динамической памятью в языке C: освоение функций "malloc", "calloc", "realloc", "free", приобретение навыков создания и использования динамических массивов, управления памятью в runtime, а также разработки практических алгоритмов обработки динамических структур данных.


    Используемые технологии и библиотеки:

    Язык программирования
C - компилируемый язык программирования.

    Библиотеки

"<stdio.h>"   Функции ввода/вывода (printf, scanf ) 
"<stdlib.h>"  Функции управления памятью (malloc, calloc, realloc, free) 
"<string.h>"  Строковые функции (strcpy, strcmp - для работы со структурами) 

    Инструменты сборки:

GCC- компилятор для языка C

    Содержание заданий:

 №1 Создание динамического массива. Выделение памяти с помощью malloc, заполнение и вывод.
 №2 Использование calloc. Выделение памяти с инициализацией нулями.
 №3 Поиск элементов. Поиск max, min, вычисление суммы элементов.
 №4 Изменение размера (realloc). Увеличение и уменьшение размера массива.
 №5 Удаление элемента. Удаление по индексу со сдвигом элементов.
 №6 Вставка элемента. Вставка в заданную позицию со сдвигом.
 №7 Массив структур. Структура Student, поиск самого старшего.
 №8 Динамический список. Полноценное меню с операциями над списком.


    Инструкция по запуску

gcc file1-8.c -o tes1-8
./test1-8

    Контрольные вопросы:

1. Что делает malloc

Функция malloc - выделяет заданное количество байт памяти.
Особенности:
• выделяет память без инициализации
• значения в памяти случайные
• при ошибке возвращает NULL

2. Чем отличается malloc от calloc

Различие между malloc и calloc

malloc:
• не инициализирует память
• принимает один аргумент
calloc:
• заполняет память нулями
• принимает два аргумента

3. Что делает realloc

Функция realloc - изменяет размер ранее выделенного блока памяти.
Особенности:
• может изменить адрес блока памяти
• сохраняет старые данные (в пределах нового размера)
• новые элементы не инициализируются
• при ошибке может вернуть NULL

4. Что произойдет при утечке памяти

Утечка памяти приводит к:

Постепенному уменьшению доступной оперативной памяти
Замедлению работы программы и системы
Возможному зависанию или завершению программы

5. Зачем нужен free

Функция free - освобождает ранее выделенную память.

6. Что произойдет при двойном освобождении памяти

Двойной вызов free для одного и того же указателя приводит к:

Программа может завершиться 
Может испортить менеджер памяти 
Данные могут повредиться
Иногда может сработать незаметно, но рискованно

7. Как проверить, что malloc вернул NULL

Нужно сравнить указатель с NULL после каждого вызова

8. В чем отличие статического и динамического массивов

Отличие статических и динамических массивов
Статические массивы:
• имеют фиксированный размер
• создаются при запуске программы
• автоматически освобождаются
Динамические массивы:
• размер задается во время выполнения
• требуют ручного управления памятью
• могут изменять размер